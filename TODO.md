# dad_proxy

## Список задумок
  - rate-limit на helloWorld (DAD_PROXY_API_HELLO)
  - Перейти на нормальный LOG_LEVEL вместо ENV.
  - Гасить туннели в случае, если корейцы катят обновку ( underMaintenance != 0 ).
  - Переподнятие туннеля в случае сетевой ошибки.
  - Ограничение доступа к /api/tunnels. Прикрыть например BasicAuth.
  - Причесать логи ( неприоритетно ).
  - Написать отдельный бэк для шаринга ( неприоритетно, так как хз сколько людей сами поднимут прокси).
  - Поддержка ipv6 для оверлея yggdrasil ( неприоритетно ).
  - Поддержка sentry для сбора ошибок ( неприоритетно ).
